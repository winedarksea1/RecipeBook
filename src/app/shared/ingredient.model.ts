export class Ingredient {
  // this is a shortcut Angular provides
  // It accomplishes the same thing as declaring
  // the variable and initiliazing them in the constructor
  constructor(public name: string, public amount: number) {}
}
